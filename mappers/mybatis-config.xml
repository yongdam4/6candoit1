<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
  <!-- settings: mybatis구동시 선언할 설정들을 작성하는 영역 -->
  <settings>
    <setting name="jdbcTypeForNull" value="NULL"/>
    <setting name="mapUnderscoreToCamelCase" value="true"/>
</settings>
  
  <!-- typeAliases : VO/DTO클래스들의 풀클래스명을 단순한 클래스명(별칭)으로 사용하기위한 별칭등록을 하는 영역 -->
  <typeAliases>
    <typeAlias type="com.semi.member.model.vo.Member" alias="Member" />
    <typeAlias type="com.semi.order.model.vo.Order" alias="Order" />
    <typeAlias type="com.semi.product.model.vo.Product" alias="Product" />
</typeAliases>

  <!-- environments : mybatis에서 연동할 db정보들을 등록하는 영역 -->
  <environments default="development">
    <environment id="development">
      <!-- 
      		transactionManager : JDBC와 MANAGED 둘중 하나의 타입을 설정할 수 있음
      		- JDBC : 트랜잭션을 내가 직접관리(수동commit)
      		- MANAGED : 트랜잭션에 대해서 어떤한 영향도 행사하지 않겠다.(자동commit)
       -->
      <transactionManager type="JDBC"/>
      <!-- 
      		dataSource는 pooled와 unpooled 둘중 하나의 타입을 선택할 수 있음(connectionPool사용여부)
      		connectionPool : Connection객체를 담아둘 수 있는 영역
      						 한번 생성된 Connection객체를 담아두면 재사용할 수 있다.
       -->
      <dataSource type="POOLED">
        <property name="driver" value="oracle.jdbc.driver.OracleDriver"/>
        <property name="url" value="jdbc:oracle:thin:@localhost:1521:xe"/>
        <property name="username" value="Jaemin"/> <!-- 본인 db계정에 맞춰서 바꿔야 함 -->
        <property name="password" value="Jaemin"/>
      </dataSource>
    </environment>
  </environments>
  
  <!-- mappers : 실행할 sql문들을 기록해둔 mapper파일들을 등록하는 영역 -->
  <mappers>
    <mapper resource="/mappers/member-mapper.xml"/>
    <mapper resource="/mappers/order-mapper.xml"/>
    <mapper resource="/mappers/product-mapper.xml"/>
  </mappers>
</configuration>