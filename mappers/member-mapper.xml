<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberMapper">
	<!-- 관리자 정보 매핑 -->
	<resultMap type="Member" id="memberResultSet">
		<result column="USER_ID" property="userId" />
		<result column="USER_PWD" property="userPwd" />
		<result column="USER_NAME" property="userName" />
		<result column="GENDER" property="gender" />
		<result column="USER_BIRTHDAY" property="birthday" />
		<result column="USER_EMAIL" property="email" />
		<result column="USER_PHONE" property="phone" />
		<result column="USER_ENROLL_DATE" property="enrollDate" />
		<result column="USER_DELFLAG" property="delFlag" />
		<result column="USER_DELETE_DATE" property="deleteDate" />
		<result column="USER_GRADE" property="grade" />
		<result column="USER_ADDRESS" property="address" />
		<result column="GRADE_ADMIN" property="admingrade" />
		<result column="USER_COIN" property="userCoin" />
	</resultMap>

	<!-- 관리자 -->
	<!-- 관리자 로그인 쿼리 추가 -->
	<select id="adminloginMember" parameterType="Member"
		resultMap="memberResultSet">
		SELECT * FROM MEMBER
		WHERE USER_ID = 'admin'
		AND USER_PWD =
		'1234'
	</select>
	<!-- 회원정보 -->
	<select id="selectAllMembers" resultMap="memberResultSet">
		SELECT
		USER_NAME,
		USER_ID,
		USER_GRADE,
		USER_PHONE,
		USER_EMAIL,
		USER_ENROLL_DATE,
		USER_DELFLAG
		FROM MEMBER
	</select>

	<!-- 회원명으로 검색하는 쿼리 -->
	<select id="adminsearchMembersByName" parameterType="String"
		resultMap="memberResultSet">
		SELECT * FROM MEMBER
		WHERE USER_NAME LIKE '%' || #{name} ||
		'%'
	</select>

	<!-- 아이디로 검색하는 쿼리 -->
	<select id="searchMembersById" parameterType="String"
		resultMap="memberResultSet">
		SELECT * FROM MEMBER
		WHERE USER_ID LIKE '%' || #{id} || '%'
	</select>

	<!-- 회원등급로 검색하는 쿼리 -->
	<select id="searchMembersByGrade" parameterType="String"
		resultMap="memberResultSet">
		SELECT * FROM MEMBER
		WHERE USER_GRADE LIKE '%' || #{grade} ||
		'%'
	</select>
	<!-- 관리자 -->
	
	  <!-- 로그인 쿼리 -->
    <select id="loginMember" parameterType="Member" resultMap="memberResultSet">
        SELECT USER_ID,
               USER_PWD,
               USER_NAME,
               GENDER,
               USER_BIRTHDAY,
               USER_EMAIL,
               USER_PHONE,
               USER_ENROLL_DATE,
               USER_DELFLAG,
               USER_DELETE_DATE,
               USER_GRADE,
               USER_ADDRESS,
               GRADE_ADMIN,
               USER_COIN
        FROM MEMBER
        WHERE USER_ID = #{userId}
          AND USER_PWD = #{userPwd}
    </select>


    <!-- 이름으로 회원 검색 쿼리 (오라클용) -->
    <select id="searchMembersByName" parameterType="String" resultMap="memberResultSet">
        SELECT USER_ID,
               USER_PWD,
               USER_NAME,
               GENDER,
               USER_BIRTHDAY,
               USER_EMAIL,
               USER_PHONE,
               USER_ENROLL_DATE,
               USER_DELFLAG,
               USER_DELETE_DATE,
               USER_GRADE,
               USER_ADDRESS,
               GRADE_ADMIN,
               USER_COIN
        FROM MEMBER
        WHERE USER_NAME LIKE '%' || #{name} || '%'
    </select>

    <!-- 전화번호로 회원 검색 쿼리 -->
    <select id="searchMembersByPhone" parameterType="String" resultMap="memberResultSet">
        SELECT USER_ID,
               USER_PWD,
               USER_NAME,
               GENDER,
               USER_BIRTHDAY,
               USER_EMAIL,
               USER_PHONE,
               USER_ENROLL_DATE,
               USER_DELFLAG,
               USER_DELETE_DATE,
               USER_GRADE,
               USER_ADDRESS,
               GRADE_ADMIN,
               USER_COIN
        FROM MEMBER
        WHERE USER_PHONE = #{phone}
    </select>
	
	
	<!-- 회원가입 -->
   <select id="insertMember" parameterType="Member">
      INSERT
        INTO MEMBER
        (
           USER_ID,
           USER_PWD,
           USER_NAME,
           GENDER,           
           USER_EMAIL,                 
           USER_ADDRESS,
           USER_PHONE,
           USER_BIRTHDAY              
                      
        )
        VALUES
        (
           #{userId},
           #{userPwd},
           #{userName},
           #{gender},                   
           #{email},
           #{address},
           #{phone},           
           #{birthday}           
        )
   </select>
   
   <!-- 회원탈퇴 -->
   <update id="deleteMember"  parameterType="Member">
      UPDATE MEMBER
         SET USER_DELFLAG = 'Y'
         SET USER_DELETE_DATE = SYSDATE
       WHERE USER_ID = #{userId}
         AND USER_PWD = #{userPwd}
         AND USER_NAME = #{userName}
         AND USER_BIRTHDAY = #{birthday}   
   </update>
   
   <!-- 아이디 중복확인 -->
   <select id="checkId" resultType="_int">
      SELECT count(*)
        FROM MEMBER
       WHERE USER_ID = #{userId}
   </select>
	

</mapper>
